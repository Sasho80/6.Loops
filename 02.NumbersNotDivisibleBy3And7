using System;

class NumbersInDefinedScope
{
    static void Main()
    {
        Console.WriteLine("Enter positive integer number.");
        int numN = int.Parse(Console.ReadLine());

        if (numN <= 0)
        {
            Console.WriteLine("Invalid Range!");
            Console.WriteLine("\n");
            return;
        }
        for (int i = 1; i <=numN; i++)
        {
            if (i % 3 == 0||i%7==0)
            {
                continue;
            }
            Console.Write("{0} ",i);
        }
        Console.WriteLine("\n");
    }
}

